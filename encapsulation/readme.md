## Encapsulation

Encapsulation is a process of wrapping the code and logic inside a class and make user see only the functional part and hide un necessay logic.
it is implemented in every oop project.
in simple words when you write code in OOP the encapsulation you use this pillar of oop automatically 