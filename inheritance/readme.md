## Inheritance

it is the most important pillar of object oriented programming it allow us to reuse code and logic that we have defined in other classes we can inherit variables methods and attributes and use them in other classes so that we not have write it again..